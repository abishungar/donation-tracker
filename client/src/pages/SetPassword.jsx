import React,{useState}from"react";import{useSearchParams,useNavigate}from"react-router-dom";import api from"../api";
export default function SetPassword(){const[q]=useSearchParams(),nav=useNavigate(),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[msg,setMsg]=useState("");async function submit(e){e.preventDefault();if(password!==confirm)return setMsg("Passwords do not match");try{await api.post("/auth/set-password",{token:q.get("token"),password});setMsg("Password saved. You can now sign in.");setTimeout(()=>nav("/login"),1200)}catch(e){setMsg(e.response?.data?.error||"Could not set password")}}return <div className="min-h-screen flex items-center justify-center px-4"><form onSubmit={submit} className="w-full max-w-sm bg-white rounded-xl shadow p-6 space-y-4"><h1 className="text-xl font-semibold">Set Password</h1><input className="w-full border rounded-lg px-3 py-2" type="password" minLength="6" placeholder="New password" required value={password} onChange={e=>setPassword(e.target.value)}/><input className="w-full border rounded-lg px-3 py-2" type="password" minLength="6" placeholder="Confirm password" required value={confirm} onChange={e=>setConfirm(e.target.value)}/>{msg&&<p className="text-sm">{msg}</p>}<button className="w-full bg-brand-600 text-white rounded-lg py-2">Save password</button></form></div>}